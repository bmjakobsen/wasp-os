add_library(usermod_screenbuf INTERFACE)

target_sources(usermod_screenbuf INTERFACE
    ${CMAKE_CURRENT_LIST_DIR}/screenbuf.c
)

target_include_directories(usermod_screenbuf INTERFACE
    ${CMAKE_CURRENT_LIST_DIR}
)

target_link_libraries(usermod INTERFACE usermod_screenbuf)
